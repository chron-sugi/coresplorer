/**
 * Widgets Layer
 *
 * Reusable layout-level UI blocks that compose features, entities, and shared UI.
 * Widgets represent named sections of a page (header, sidebar, layout, etc.)
 *
 * @module widgets
 */

export { Header } from './header';
export { Layout } from './layout';
