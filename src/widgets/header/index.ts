/**
 * Header Widget
 *
 * Application header with navigation, search slot, and global actions.
 * Composes shared UI primitives and entity UI components.
 *
 * @module widgets/header
 */
export { Header } from './ui';
