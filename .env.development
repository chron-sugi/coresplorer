# Base Path Configuration
# For local development, use root path
# For GitHub Pages, use "/coresplorer/"
VITE_BASE_PATH=/

# Data File Paths (relative to BASE_PATH)
# These are used in static mode when VITE_USE_SPLUNK_API=false
VITE_DATA_META_PATH=data/meta.json
VITE_DATA_INDEX_PATH=data/index.json
VITE_DATA_GRAPH_PATH=data/graph.json
VITE_DATA_OBJECTS_PATH=data/nodes

# Enable Mock Service Worker for development
# Set to 'true' to use MSW for API mocking in the browser
# Useful for testing without a backend or simulating errors/delays
VITE_ENABLE_MSW=false

# Splunk Server Configuration (Development)
# These values are used to connect to your Splunk instance
VITE_SPLUNK_HOST=localhost
VITE_SPLUNK_PORT=8089
VITE_SPLUNK_PROTOCOL=https

# Set to 'true' to enable fetching data from the Splunk API
# If 'false', the app will load static JSON files from the public folder
VITE_USE_SPLUNK_API=false

# Optional: Splunk API Token (if required for API access)
# VITE_SPLUNK_TOKEN=your-development-token-here

# Splunk Web UI Configuration (for "View in Splunk" deep links)
# Note: Web UI port is typically 8000, different from API port (8089)
VITE_SPLUNK_WEB_HOST=localhost
VITE_SPLUNK_WEB_PORT=8000
VITE_SPLUNK_WEB_PROTOCOL=https
