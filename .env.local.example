# Local Environment Variables Template
# Copy this file to .env.local and fill in your values
# .env.local is gitignored and will override .env.development values

# Base Path Configuration
# For local development, typically use "/" (root)
# For custom deployment paths, set accordingly (e.g., "/my-app/")
VITE_BASE_PATH=/

# Data File Paths (relative to BASE_PATH)
# Usually you don't need to change these
VITE_DATA_META_PATH=data/meta.json
VITE_DATA_INDEX_PATH=index.json
VITE_DATA_GRAPH_PATH=graph.json
VITE_DATA_OBJECTS_PATH=objects

# Splunk Server Configuration
# Replace these with your local Splunk instance details
VITE_SPLUNK_HOST=localhost
VITE_SPLUNK_PORT=8089
VITE_SPLUNK_PROTOCOL=https

# Set to 'true' to enable fetching data from the Splunk API
# If 'false', the app will load static JSON files from the public folder
VITE_USE_SPLUNK_API=false

# Optional: Splunk API Token for authenticated requests
# Get this from your Splunk instance: Settings > Tokens
VITE_SPLUNK_TOKEN=

# Splunk Web UI Configuration (for "View in Splunk" deep links)
# Note: Web UI port is typically 8000, different from API port (8089)
VITE_SPLUNK_WEB_HOST=localhost
VITE_SPLUNK_WEB_PORT=8000
VITE_SPLUNK_WEB_PROTOCOL=https

# MSW Configuration (set to 'true' to use Mock Service Worker)
VITE_ENABLE_MSW=false
